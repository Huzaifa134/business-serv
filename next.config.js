/** @type {import('next').NextConfig} */
const nextConfig = {
  images:{
    domain:["cdn.sanity.io"]
  }
}

module.exports = nextConfig

module.exports = {
    images: {
      domains: ['images.unsplash.com'],
    },
  }
  